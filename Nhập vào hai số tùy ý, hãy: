#include <iostream>
 using namespace std;
 int main() {
 	int a,b;
 	cout<<"nhap hai so a va b : ";
 	cin>>a>>b;
 	if(a>b) {
 		cout<<a<<">"<<b<<endl;
	 }
	 else{
	 	cout<<a<<"<"<<b<<endl;
	 }
 	return 0;
 }
